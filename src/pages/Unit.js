import React, { Fragment } from "react";
import MainFilter from "../components/MainFilter/MainFilter";

const Unit = () => {
  return (
    <Fragment>
      <MainFilter />
    </Fragment>
  );
};

export default Unit;
